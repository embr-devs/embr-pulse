platform: nodejs
platformVersion: "20"
autoDeploy: true

run:
  port: 3000
  startCommand: "npm start"

healthCheck:
  enabled: true
  path: "/api/health"
  expectedStatusCode: 200

database:
  enabled: true
  engine: postgres
  framework: raw
  schema: db/schema.sql
